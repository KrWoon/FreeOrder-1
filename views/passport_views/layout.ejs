<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <h1><a href="/home"> Free Order</a></h1>
        <% if(login) { %>
            <p> <a href="/auth/logout">Logout</a> </p>
        <% } else { %>
            <p> 
                <a href="/auth/login">Login</a>
                <a href="/auth/register">Register</a>
            </p>
        <% } %>        
        <!-- <ul>
            <% topics.forEach(function(item, index) { %>
            <li><a href="/home/<%=item.uid%>"> <%=item.email%> </a></li>
            <% }); %>        
        </ul> -->