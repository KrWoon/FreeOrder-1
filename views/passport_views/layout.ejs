<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <h1><a href="/"> Free Order</a></h1>  
        <% if(login) { %>
            <p>
                I am <%= login.Email %>
            </p>
            <p> 
                <a href="/auth/logout">Logout</a>
            </p>
            <% if(login.Power == 0) { %>
            <p>
                <a href="/form/apply">Apply Restaurant</a>
            </p>
            <% } else { %>
                <a href="/form/permit">Permit Restaurant</a>
            <% } %>
        <% } else { %>
            <p> 
                <a href="/auth/login">Login</a>
            </p>
            <p>
                <a href="/auth/register">Register</a>
            </p>
        <% } %>